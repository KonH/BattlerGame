namespace GameLogics.Shared.Model {
	/// <summary>
	/// For simplify usage, because of excessive 'Type' serialization and presentation converting
	/// </summary>
	public enum ItemType {
		Weapon,
		Armor,
	}
}